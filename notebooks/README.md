# notebooks/

One-off utility scripts. Not part of the runtime path — run manually when needed.

## Files

| File | Role |
|---|---|
| `load_dataset.py` | Downloads `ImperialCollegeLondon/health_fact` from HuggingFace Hub and prints a schema summary; run once to inspect the raw dataset before it was exported to parquet |
